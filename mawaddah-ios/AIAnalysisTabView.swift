import SwiftUI

struct AIAnalysisTabView: View {
  var body: some View {
    ZStack {
      Color.appBackground.ignoresSafeArea()
      Text("AI Analysis")
        .font(.title)
        .foregroundColor(.black)
    }
  }
}
