import SwiftUI

struct AIAnalysisTabView: View {
    var body: some View {
        ZStack {
            Color.purple.opacity(0.3).ignoresSafeArea()
            Text("AI Analysis")
                .font(.title)
                .foregroundColor(.black)
        }
    }
} 