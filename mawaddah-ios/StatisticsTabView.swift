import SwiftUI

struct StatisticsTabView: View {
    var body: some View {
        ZStack {
            Color.purple.opacity(0.3).ignoresSafeArea()
            Text("Statistics")
                .font(.title)
                .foregroundColor(.black)
        }
    }
} 